# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# commandenp0s25:
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/usr/lib/i3blocks/$BLOCK_NAME
separator_block_width=15
markup=none

# Volume indicator
#
# The first parameter sets the step (and units to display)
# The second parameter overrides the mixer selection
# See the script for details.
#[volume-pulseaudio]
#command=/opt/i3blocks-contrib/volume-pulseaudio/volume-pulseaudio
#interval=once
#signal=1

# Memory usage
#
# The type defaults to "mem" if the instance is not specified.
[memory]
command=/opt/i3blocks-contrib/memory/memory
label=MEM
interval=30

[memory]
command=/opt/i3blocks-contrib/memory/memory
label=SWAP
instance=swap
interval=30

# Disk usage
#
# The directory defaults to $HOME if the instance is not specified.
# The script may be called with a optional argument to set the alert
# (defaults to 10 for 10%).
[disk]
label=
instance=/
interval=30

# Network interface monitoring
#
# If the instance is not specified, use the interface used for default route.
# The address can be forced to IPv4 or IPv6 with -4 or -6 switches.
[iface]
instance=wlp3s0
color=#00FF00
interval=10
separator=true
label=


[iface]
instance=enp0s31f6
color=#00FF00
interval=10
separator=true
label=

[bandwidth3]
command=/opt/i3blocks-contrib/bandwidth3/bandwidth3
interval=persist
label=
markup=pango

[arch-update]
command=/opt/i3blocks-contrib/arch-update/arch-update
interval=3600
markup=pango

# CPU usage
#
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
[cpu_usage]
label=CPU
interval=10
#min_width=CPU: 100.00%
#separator=false

[battery2]
command=/opt/i3blocks-contrib/battery2/battery2
markup=pango
interval=30

#[docker]
#command=/opt/i3blocks-contrib/docker/docker
#label=
#color=#0db7ed
#interval=10

[load_average]
label=Load
interval=10

# Battery indicator
#
# The battery instance defaults to 0.
[battery]
#label=BAT
label=⚡
instance=1
interval=30
